{
  "name": "nulinks",
  "version": "0.0.1",
  "private": true,
  "description": "MyNortheastern but faster!",
  "scripts": {
    "install": "cd nulinks-site && npm install && cd ../nulinks-common && npm install",
    "test": "cd nulinks-common && ./node_modules/.bin/jest",
    "test:update": "cd nulinks-common && ./node_modules/.bin/jest --updateSnapshot",
    "prettier:write": "prettier \"**/*.{js,css}\" --write",
    "lint": "prettier \"**/*.{js,css}\" --list-different"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kj800x/nulinks.git"
  },
  "author": "Kevin Johnson",
  "license": "MIT",
  "devDependencies": {
    "prettier": "1.18.2"
  }
}

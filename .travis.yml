language: node_js
node_js:
  - "node"
jobs:
  include:
    - stage: test
      script: bash .bin/build-site.sh
      script: bash .bin/test-common.sh
deploy:
  skip_cleanup: true
  provider: script
  script: bash .bin/deploy.sh
  on: 
    branch: master